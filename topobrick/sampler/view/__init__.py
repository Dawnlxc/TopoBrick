"""What the agent is shown, and how its answer is resolved.

    spine      The target's ancestry up-cone: its "you are here".
    context    Renders the egocentric view (anchors, pullable class histograms,
               building-global drivers), and resolves the agent's pull requests
               back into concrete leaf URIs.
"""
